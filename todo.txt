# FUTO Keyboard - Feature & Bug Checklist

## COMPLETED

- [x] **Clipboard search focus issue** - Fixed sorting when search box gains/loses focus (ActionTextEdit.kt)
- [x] **Voice Input Bottom Bar Mode** - Floating pill UI like Gboard with mic, status text, animations
- [x] **Merge branch with upstream FUTO keyboard**
- [x] **Default keyboard prompt** - Ask to set as default input when app opens
- [x] **Voice button activation in floating bar** - Fixed mic button to start voice recognition
- [x] **Voice button stops recognition** - Mic button now toggles start/stop
- [x] **AI reply system prompts** - Multiple system prompts selectable from keyboard
- [x] **T9 layout bug** - Fixed by disabling T9 layout
- [x] **Voice transcripts to clipboard** - Copy voice input results to clipboard
- [x] **Clipboard import/export** - Import and export pinned clipboard items
- [x] **Clipboard image support** - Support screenshots and images in clipboard
- [x] **Clipboard screenshot display** - Show recent screenshots in clipboard
- [x] **Floating bar keyboard conflict** - Hide floating bar when keyboard expands
- [x] **Settings search field expansion** - Search field expands for multiline text
- [x] **Reset theme to default** - Added reset theme functionality
- [x] **Voice button position** - Moved voice recognition button slightly left
- [x] **Floating bar text navigation** - Slide to navigate text like spacebar swipe
- [x] **Floating bar editing buttons** - Added undo (left), backspace, redo (right)
- [x] **Clipboard entry layout** - Text displayed between pin and close icons

## PENDING

- [ ] **Voice recognition button** - pill mode, floating bar mode, if i click on the voice recognition button second time, it does not paste into the text area. but it the clipboard i see it combines the text with the previous audio....i think it is sending combined audio to the server.

- [ ] **Toast messages** - when toast messages happen, copying or other thing when keyboard is on, it appears on top of the keyboard obscuring some letters. i don't want that to happen. we can have/force the toast message above the keyboard.


- [ ] we should be able to skip the enable accessibility service grant permission at setup. later we should be able rerun the setup from inside  the settings section.



- [ ] Add a sessionId guard to ignore stale callbacks (high value)
Why: Even after reset()/cancelAll(), there can be edge cases where an old coroutine finishes late and calls listener.finished() or partialResult() for the previous session. This can look like “2nd run commits old text” or “mixed results”.
- How (concept):
    - In AudioRecognizer, add private var sessionId: Long = 0
    - Increment sessionId in reset() and at the start of startRecording()
    - Capture the current id inside runModel() and before each listener.partialResult(...) / listener.finished(...), verify the id still matches.


- [ ] **Keyboard horizontal positioning** - Move keyboard left/right when resized (not just centered)
- [ ] **Settings search image paste** - Support pasting images in settings search field
- [ ] **Settings search keyboard functions** - Fix keyboard functions not working in search field
- [ ] **Theme color selection** - Improve basic theme color picker

---
**Build command:** `.\gradlew.bat assemblePlaystoreRelease`

see if any errors
fix the errors
then suggest git add commit and push with appropriate message

