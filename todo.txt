feature: sort clipboard search focus issue - need to track when search box gains/loses focus and update sorting accordingly
[DONE] - Fixed in ActionTextEdit.kt using LaunchedEffect for reliable focus

feature: Voice Input Bottom Bar Mode (like Gboard)
[IN PROGRESS] - Implementation complete, needs testing
- Added VOICE_INPUT_BOTTOM_BAR_MODE setting in VoiceInputSettingKeys.kt
- Added UI toggle in VoiceInput.kt settings page
- Added strings in strings-uix.xml
- Created VoiceInputBottomBarWindow class in VoiceInputAction.kt
- Features:
  - Minimal floating pill bar at bottom (72dp height)
  - Keyboard is HIDDEN, bar REPLACES keyboard (onlyShowAboveKeyboard = false)
  - fixedWindowHeight = 72.dp to keep it compact
  - Mic button with pulse animation when listening
  - Status text showing "Listening…" or partial transcription
  - Close button to exit voice mode



First combine out branch with the actual futo keyboard by futo!!





feature: the keyboard app if it is not set default ask if we want to set it as default input everytime we open the app!



feature: should be able to change the system prompt for ai reply from inside the keyboard without  going to keyboard app



feature: theme colour selection is still basic!





auto hotkey caps lock plus backspace is equal to control backspace.